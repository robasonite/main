# 2020-11-20

I added a new `restore` command that can be used to restore brightness and volume to *sane* values. Use the new command from your `.xinitrc` or other auto-start file and edit `~/.deskbuddyrc` to set your preferred values.
